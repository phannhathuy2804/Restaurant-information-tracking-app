<!-- 2nd iteration-->

1. Product Owner(PO): Nhat huy Phan(James)
2. Scrum Master(SM): Justin Gregg(Justin)
3. Repo Manager(RM): Ching Hang Lam(Richard)
4. Team Member(TM): Kodai Hiraishi(Kodai)

Notes for TA:
1. The map is called before asking for permissions, so the map loads in a random location on start up.
Toggling the map view or list view corrects this error.

2. After getting the write permission, it can't download anything, requires to reload